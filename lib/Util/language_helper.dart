import 'dart:ui';

import 'package:flutter/material.dart';

class LanguageHelper extends ChangeNotifier {
  static const Locale kEnglishLocale = Locale('en', 'US');
  static const Locale kArabicLocale = Locale('ar', 'AR');
}
